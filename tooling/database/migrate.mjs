import {readFileSync,writeFileSync,mkdirSync} from 'node:fs';import {resolve} from 'node:path';import {execFileSync} from 'node:child_process';
const config=JSON.parse(readFileSync('dist/server/wrangler.json','utf8'));config.main=resolve('dist/server/index.js');if(config.assets)config.assets.directory=resolve('dist/client');for(const db of config.d1_databases??[])db.migrations_dir=resolve('database/migrations');mkdirSync('.sites-runtime',{recursive:true});const path=resolve('.sites-runtime/local-migrations.json');writeFileSync(path,JSON.stringify(config,null,2));
execFileSync(process.execPath,['--import','./tooling/runtime/sites-env.mjs','./node_modules/wrangler/bin/wrangler.js','d1','migrations','apply','DB','--local','--config',path,'--persist-to','.wrangler/state'],{stdio:'inherit'});
